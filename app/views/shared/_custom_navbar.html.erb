<nav class="navbar navbar-dark mb-0 p-0 "style="background-color: #FBC74C">
  <div class="container-fluid">
    <a id="logo" class="navbar-brand fw-bolder text-dark" href="/"><%= image_tag "favicon.png", height: 50, width: 50,  alt: "marker" %></a>
    <span class="navbar-toggler p-0 rounded-10"d data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" >
      <% if current_user %>
      <div class="user_avatar1"><%= cl_image_tag current_user.photo.key, height: 45, width: 45  %></div>
      <% else %>
      <i class="fa-solid fa-user"></i>
      <% end %>
    </span>
    <div class="offcanvas offcanvas-end bg-light shadow-lg p-3 mb-5 bg-body rounded text-center" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
      <div class="offcanvas-header d-block bg-info">
        <h5 id="offcanvasNavbarLabel1">KIBONBAZ</h5>
        <% if current_user %>
        <div class="user_avatar2"><%= cl_image_tag current_user.photo.key, height: 80, width: 80 %></div>
        <% else %>
        <i class="fa-solid fa-user"></i>
        <% end %>
        <br>
        <% if user_signed_in?%>
          <small class="offcanvas-title text-dark fw-bolder shadow-lg p-3 mb-5  bg-white rounded-pill text-dark text-dark" id="offcanvasNavbarLabel"> <%=current_user.username %> logged in </small>
        <% end %>
        <button type="button" class="btn-close btn-close-black text-reset position-absolute top-0 end-0 m-1" data-bs-dismiss="offcanvas" aria-label="Close"></button>
      </div>
      <div class="offcanvas-body fw-bolder bg-info">
        <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
          <li class="nav-item shadow-lg p-2 mb-3 bg-white rounded-pill">
            <%= link_to "Find a restaurant", restaurants_path, class: "nav-link text-dark text-dark" %>
          </li>

          <% if user_signed_in? && current_user.owner == true %>
            <li class="nav-item shadow-lg p-2 mb-3 bg-white rounded-pill">
              <%= link_to "My Restaurants","" , class: "nav-link text-dark text-dark" %>
            </li>
          <% end %>
          <% if user_signed_in? %>
            <li class="nav-item shadow-lg p-2 mb-3 bg-white rounded-pill">
              <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete},class:" nav-link text-dark text-decoration-none"%>
            </li>
          <% else %>
            <li class="nav-item shadow-lg p-2 mb-3 bg-white rounded-pill">
              <%= link_to "Login/Sign up", new_user_session_path, class: "nav-link text-dark" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
